<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <record id="view_move_form_facturapi_fe" model="ir.ui.view">
      <field name="name">account.move.form.facturapi.fe</field>
      <field name="model">account.move</field>
      <field name="inherit_id" ref="account.view_move_form"/>
      <field name="arch" type="xml">
        <xpath expr="//header" position="inside">
          <field name="connector_facturapi_state" invisible="1"/>
          <button name="action_send_to_dian" type="object"
                  string="Send to DIAN"
                  class="oe_highlight"
                  invisible="not company_id.connector_fe_enabled or move_type not in ('out_invoice', 'out_refund', 'out_debit') or state != 'posted'"/>
          <button name="action_check_dian_status" type="object"
                  string="Check DIAN Status"
                  invisible="not connector_facturapi_state or connector_facturapi_state == 'accepted' or connector_facturapi_state == 'rejected'"/>
          <button name="action_cancel_dian" type="object"
                  string="Cancel in DIAN"
                  invisible="connector_facturapi_state != 'accepted'"/>
        </xpath>
        <xpath expr="//page[@id='other_tab']" position="after">
          <page id="dian_tab" string="DIAN"
                invisible="not company_id.connector_fe_enabled or not connector_facturapi_state">
            <group>
              <group string="Status">
                <field name="connector_facturapi_state"/>
                <field name="connector_cufe"/>
              </group>
              <group string="Resolution" invisible="not connector_sequence_range_id">
                <field name="connector_sequence_range_id"/>
              </group>
            </group>
            <group string="QR Code" invisible="not connector_qr_code">
              <field name="connector_qr_code" widget="image" class="w-25"/>
            </group>
          </page>
        </xpath>
      </field>
    </record>
  </data>
</odoo>
